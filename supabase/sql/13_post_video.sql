-- ============================================================
-- [13] 게시글 동영상 첨부 (video_url)
-- ------------------------------------------------------------
-- 사용법: 전체 복사 → Supabase SQL Editor → Run.
-- (gif는 image_urls로 처리되므로 별도 컬럼 불필요)
-- ============================================================

alter table public.posts add column if not exists video_url text;
